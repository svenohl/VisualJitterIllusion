display <- matrix(nrow=screen.x,ncol=screen.y)
display <- matrix(nrow=100,ncol=100)
display
display <- matrix(ifelse(runif(screen.x*screen.y)<0.5,0,1),nrow=screen.x,ncol=screen.y)
screen.x <- 10
screen.y<-100
screen.y<-10
display <- matrix(ifelse(runif(screen.x*screen.y)<0.5,0,1),nrow=screen.x,ncol=screen.y)
display
image(display)
par(mar=c(0,0,0,0))
image(display)
